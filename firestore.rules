rules_version = '2';
// All reads and writes go through the REST API (Admin SDK), which enforces
// server-side authorisation per SRS §6 and §24. Clients never talk to Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
